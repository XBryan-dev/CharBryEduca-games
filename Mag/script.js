const games=window.EVRION_GAMES||[],grid=document.getElementById('grid'),toast=document.getElementById('toast');function msg(x){toast.textContent=x;toast.classList.add('show');clearTimeout(window.tm);window.tm=setTimeout(()=>toast.classList.remove('show'),2200)}function openGame(g){if(g.status!=='available')return msg('This game is coming soon.');if(!g.url||g.url.includes('YOUR_QUIZ_URL_HERE'))return msg('Add this game URL in games.js.');location.href=g.url}function render(list){grid.innerHTML='';document.getElementById('empty').style.display=list.length?'none':'block';list.forEach((g,i)=>{let ok=g.status==='available',c=document.createElement('article');c.className='card';c.innerHTML=`<div class="art ${ok?'liveart':'soonart'}">${ok?'<span class="mark">'+(g.type==='quiz'?'?':'+')+'</span>':'<strong>'+String(i+1).padStart(2,'0')+'</strong>'}<span class="code">EVR / ${String(i+1).padStart(3,'0')}</span></div><div class="copy"><span class="status ${ok?'live':''}">${ok?'AVAILABLE':'COMING SOON'}</span><h3>${safe(g.title)}</h3><p>${safe(g.description)}</p><button class="enter ${ok?'':'disabled'}">${ok?'Play now':'Keep watching'} →</button></div>`;c.querySelector('button').onclick=()=>openGame(g);grid.appendChild(c)})}function safe(s){return String(s||'').replace(/[&<>"']/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[x]))}document.getElementById('count').textContent=String(games.length).padStart(2,'0');render(games);let f=games.find(x=>x.featured)||games[0];if(f){ftitle.textContent=f.title;fdesc.textContent=f.description||'';ftype.textContent=(f.type||'GAME').toUpperCase();fplay.onclick=()=>openGame(f)}document.querySelectorAll('[data-f]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-f]').forEach(x=>x.classList.remove('sel'));b.classList.add('sel');render(b.dataset.f==='all'?games:games.filter(g=>g.categories?.includes(b.dataset.f)))});profile.onclick=()=>msg('Profiles are coming later.');eventsBtn.onclick=()=>msg('Events are part of the roadmap.');